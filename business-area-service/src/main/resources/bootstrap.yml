
spring:
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}
      name: business-area-service
      fail-fast: false
      retry:
        max-attempts: 30