
spring:
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}
      name: research-project-service
      fail-fast: true
      retry:
        max-attempts: 30